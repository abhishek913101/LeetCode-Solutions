class Solution:
    def concatenatedBinary(self, n: int) -> int:
        l = []
        for i in range(1,n+1):
            ans = str(bin(i))
            l.append(ans[2:])
        
        ans = ""
        for i in l:
            ans += i
        ans = int(ans,2)


        return ans%(10**9+7)